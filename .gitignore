# Real scraped data — never commit, always regenerated locally.
# application/data/sample/ is deliberately NOT listed here — it's committed
# synthetic data (see application/scripts/generate_sample_data.py), the
# whole point of which is to be present with zero setup.
application/data/exports/
application/viz/output/
application/viz/story/data/

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
